diff --git a/src/sabr/model.py b/src/sabr/model.py
index 5388312..70bfa64 100644
--- a/src/sabr/model.py
+++ b/src/sabr/model.py
@@ -172,10 +172,10 @@ class ProteinFeatures(hk.Module):
 
     def _get_rbf(self, A, B, E_idx):
         """Compute RBF features between two atom types."""
-        D_A_B = jnp.sqrt(
-            jnp.sum((A[:, :, None, :] - B[:, None, :, :]) ** 2, -1) + 1e-6
+        B_neighbors = gather_nodes(B, E_idx)
+        D_A_B_neighbors = jnp.sqrt(
+            jnp.sum((A[:, :, None, :] - B_neighbors) ** 2, -1) + 1e-6
         )
-        D_A_B_neighbors = gather_edges(D_A_B[:, :, :, None], E_idx)[:, :, :, 0]
         RBF_A_B = self._rbf(D_A_B_neighbors)
         return RBF_A_B
 
