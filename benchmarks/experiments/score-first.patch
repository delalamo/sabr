diff --git a/src/sabr/alignment.py b/src/sabr/alignment.py
index 3b13432..a330718 100644
--- a/src/sabr/alignment.py
+++ b/src/sabr/alignment.py
@@ -146,6 +146,9 @@ _AFFINE_ALIGNMENT = jax.jit(
         (0, 0, None, None, None, None),
     )
 )
+_AFFINE_SCORES = jax.jit(
+    jax.vmap(_affine_score, (0, 0, None, None, None, None))
+)
 
 
 @functools.cache
@@ -309,8 +312,8 @@ def _affine_gap_penalty(
     return gap_open + (length - 1) * gap_extend
 
 
-def _align_references(query, references, mode):
-    """Batch single-domain candidates, masking padding beyond both anchors."""
+def _reference_similarities(query, references):
+    """Build a padded candidate batch with true lengths including anchors."""
     reference_lengths = [len(reference) + 2 for reference in references]
     padded = np.zeros(
         (len(references), max(reference_lengths), query.shape[1]),
@@ -324,6 +327,24 @@ def _align_references(query, references, mode):
     similarity = jnp.einsum(
         "ia,nja->nij", jnp.asarray(query), jnp.asarray(padded)
     )
+    return similarity, lengths, reference_lengths
+
+
+def _score_references(query, references, mode):
+    """Score all single-domain candidates without a backward pass."""
+    similarity, lengths, _ = _reference_similarities(query, references)
+    gap_extend, gap_open = load_gap_penalties(mode)
+    scores = _AFFINE_SCORES(
+        similarity, lengths, constants.DEFAULT_TEMPERATURE,
+        gap_extend, gap_open, -1,
+    )
+    finite = jnp.all(jnp.isfinite(similarity), axis=(1, 2))
+    return np.asarray(scores), np.asarray(finite)
+
+
+def _align_references(query, references, mode):
+    """Batch single-domain candidates, masking padding beyond both anchors."""
+    similarity, lengths, reference_lengths = _reference_similarities(query, references)
     gap_extend, gap_open = load_gap_penalties(mode)
     scores, soft_alignment = _AFFINE_ALIGNMENT(
         similarity,
@@ -389,21 +410,19 @@ def align(
         candidates = ("K",)
     else:
         candidates = tuple(chain_type.split(","))
-    batched = {}
+    scored = False
     if len(candidates) > 1 and all(
         len(candidate) == 1 for candidate in candidates
     ):
-        batched = dict(
-            zip(
-                candidates,
-                _align_references(
-                    query,
-                    [references[candidate][0] for candidate in candidates],
-                    mode,
-                ),
-                strict=True,
-            )
+        scores, finite = _score_references(
+            query, [references[candidate][0] for candidate in candidates], mode
         )
+        for candidate, score, valid in zip(candidates, scores, finite, strict=True):
+            if not np.isfinite(score) or not valid:
+                raise ValueError(f"{candidate} reference produced a non-finite alignment.")
+            LOGGER.info("%s reference score: %.4f", candidate, score)
+        candidates = (candidates[int(np.argmax(scores))],)
+        scored = True
     best = None
     for candidate in candidates:
         if candidate not in references:
@@ -411,9 +430,7 @@ def align(
                 references, candidate
             )
         reference, positions = references[candidate]
-        if candidate in batched:
-            reduced, similarity, score = batched[candidate]
-        elif len(candidate) > 1:
+        if len(candidate) > 1:
             free_gap_boundaries = tuple(
                 sum(
                     position <= domain_index * constants.IMGT_MAX_POSITION
@@ -451,7 +468,7 @@ def align(
                 terminal_penalty,
                 selection_score,
             )
-        else:
+        elif not scored:
             LOGGER.info("%s reference score: %.4f", candidate, score)
         if best is None or selection_score > best[0]:
             best = (
diff --git a/tests/test_math.py b/tests/test_math.py
index 3e67e90..8918b4b 100644
--- a/tests/test_math.py
+++ b/tests/test_math.py
@@ -310,6 +310,16 @@ def test_auto_reference_ties_resolve_in_h_k_l_order(monkeypatch):
         "sabr.alignment.load_references",
         lambda noise, mode, scfv=False: references,
     )
+    monkeypatch.setattr(
+        "sabr.alignment._score_references",
+        lambda query, references, mode: (np.ones(3), np.ones(3, dtype=bool)),
+    )
+    monkeypatch.setattr(
+        "sabr.alignment._align_reference",
+        lambda query, reference, mode: (
+            np.ones((len(query), 1)), np.zeros((len(query), 3)), 1.0
+        ),
+    )
     monkeypatch.setattr(
         "sabr.alignment._align_references",
         lambda query, references, mode: [
